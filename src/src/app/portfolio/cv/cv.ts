import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'app-cv',
  imports: [RouterLink],
  templateUrl: './cv.html',
})
export class Cv {}
